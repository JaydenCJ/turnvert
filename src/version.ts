/** Package version, mirrored from package.json (asserted equal by a test). */
export const VERSION = "0.1.0";

/** Version of the normalized event schema this build emits and validates. */
export const SCHEMA_VERSION = 1;
